const toggleButton = document.getElementById('toggle-btn');
const sidebar = document.getElementById('sidebar');

function toggleSidebar(){
  sidebar.classList.toggle('close')
  toggleButton.classList.toggle('rotate')

  closeAllSubMenus()
}

function toggleSubMenu(button){

  if(!button.nextElementSibling.classList.contains('show')){
    closeAllSubMenus()
  }

  button.nextElementSibling.classList.toggle('show')
  button.classList.toggle('rotate')

  if(sidebar.classList.contains('close')){
    sidebar.classList.toggle('close')
    toggleButton.classList.toggle('rotate')
  }
}

function closeAllSubMenus(){
  Array.from(sidebar.getElementsByClassName('show')).forEach(ul => {
    ul.classList.remove('show')
    ul.previousElementSibling.classList.remove('rotate')
  })
}

// ..... modal-prife...

let ModaEl = document.querySelector(".showcon-modal")
const containerEl  = document.querySelector(".container-modale");
const profilEl  = document.querySelector(".profil");
const mainEL = document.querySelector("main");

profilEl.addEventListener('click',()=>{
  containerEl.classList.toggle("showcon-modal");
  
})

let hrsE = document.getElementById("hrs");
        let mainE = document.getElementById("min");
        let secE = document.getElementById("sec");


//   setInterval(()=>{

//     let Times = new Date();
      

//         hrsE.innerText = (Times.getHours()<10?"0":"") +Times.getHours();
//         mainE.innerHTML = (Times.getMinutes()<10?"0":"")+Times.getMinutes();
//         secE.innerHTML = (Times.getSeconds()<10?"0":"") + Times.getSeconds()


//   },1000)


  setInterval(() => { 
    let Times = new Date();
    

    if (Times.getHours() < 10) {
        hrsE.innerText = "0" + Times.getHours();
    } else {
        hrsE.innerText = Times.getHours();
    }
    

    if (Times.getMinutes() < 10) {
        mainE.innerHTML = "0" + Times.getMinutes();
    } else {
        mainE.innerHTML = Times.getMinutes();
    }
    
  
    if (Times.getSeconds() < 10) {
        secE.innerHTML = "0" + Times.getSeconds();
    } else {
        secE.innerHTML = Times.getSeconds();
    }
}, 1000);



// ... pop up ka header ka



